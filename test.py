from db import db

# project = 'WMD蝕刻工程'
# sql = f"SELECt DISTINCT spec_code FROM sfdb.rms_spec_flat WHERE project = '{project}'"
# with db(dict_cursor=True) as (_, cur):
#     cur.execute(sql)
#     print([s["spec_code"] for s in cur.fetchall()])

# project = 'WMD蝕刻工程'
# sql = f"SELECt DISTINCT spec_code FROM sfdb.rms_spec_flat WHERE project = '{project}'"
# with db(dict_cursor=True) as (_, cur):
#     cur.execute(sql)
#     print([s["spec_code"] for s in cur.fetchall()])